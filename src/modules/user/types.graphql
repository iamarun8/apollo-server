type User {
    id: ID!
    name: String!
    email: String!
    role: String
}

input loginInput {
    email: String!
    password: String!
}

type onLogin {
    code: String!
    Message: String!
    Data: String!
}