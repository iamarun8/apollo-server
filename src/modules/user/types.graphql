type User {
    id: ID!
    name: String!
    email: String!
    role: String
}

input loginInput {
    email: String!
    password: String!
}

type onLogin {
    code: String!
    Message: String!
    Data: String!
}

type trainee {
    _id: ID!
    name: String!
    email: String!
    role: String
    originalId: ID
}

input UpdateUser {
    originalId: ID!
    name: String
    email: String
    password: String
}