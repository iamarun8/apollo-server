type Query {
    getMyProfile: User!
    getAllTrainees(user: fetch): [trainee]
    getTrainee(id: ID!): User!
}

type Mutation {
    createTrainee(user: CreateUserInput): User!
    updateTrainee(User: UpdateUser): User!
    deleteTrainee(id: ID!): String!
    loginUser(payload: loginInput!): String
}

type Subscription {
    traineeAdded: User!
    traineeUpdated: User!
    traineeDeleted: ID!
}